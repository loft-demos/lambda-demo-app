apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: lambda-demo-app-pr
  namespace: argocd
spec:
  generators:
  - pullRequest:
      github:
        appSecretName: loft-demo-org-cred
        # The GitHub organization or user.
        owner: loft-demos
        # The Github repository
        repo: lambda-demo-app
        # (optional) use a GitHub App to access the API instead of a PAT.
        #appSecretName: github-app-repo-creds
        # Labels is used to filter the PRs that you want to target. (optional)
        labels:
        - preview-cluster-ready
      requeueAfterSeconds: 30
  template:
    metadata:
      name: 'lambda-demo-app-{{branch}}-pr{{number}}'
    spec:
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
      source:
        repoURL: 'https://github.com/loft-demos/lambda-demo-app.git'
        targetRevision: '{{head_sha}}'
        path: helm-chart/
        helm:
          parameters:
          - name: "image.repository"
            value: ghcr.io/loft-demos/lambda-demo-app-demo-app
          - name: "image.tag"
            value: "{{head_short_sha}}"
          - name: "image.args.text"
            value: "Hello from lambda-demo-app pr-{{number}} commit {{head_short_sha}}"
          - name: "ingress.hosts[0].host"
            value: lambda-demo-app-pr-{{number}}-lambda-demo.us.demo.dev
          - name: ingress.hosts[0].paths[0].backend.service.name
            value: juliet-app
          - name: ingress.hosts[0].paths[0].backend.service.port.name
            value: http
          - name: ingress.hosts[0].paths[0].path
            value: /
          - name: ingress.hosts[0].paths[0].pathType
            value: prefix
          - name: "ingress.tls[0].hosts[0]"
            value: lambda-demo-app-pr-{{number}}-lambda-demo.us.demo.dev
      project: "default"
      destination:
        server: https://lambda-demo.us.demo.dev/kubernetes/project/default/virtualcluster/lambda-demo-app-pr-{{number}}
        namespace: preview-hello-world-app
      info:
        - name: Preview App Link
          value: >-
            https://lambda-demo-app-pr-{{number}}-lambda-demo.us.demo.dev
        - name: GitHub PR
          value: >-
            https://github.com/loft-demos/lambda-demo-app/pull/{{number}}
